export const INSTANCE_DOMAIN = "woem.men"; // domain of the mastodon instance
export const USE_INTERNET_IMAGE = false;   // experimental: allow use of cached internet images (e.g. for profile pictures)
export const INTERNET_IMAGE_MODE = "tga";  // png for simulator, tga for real device
export const POST_LIMIT_PER_PAGE = 5;      // number of posts to fetch per page on the timeline
